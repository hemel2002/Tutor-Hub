<!DOCTYPE html>
<html lang="en" data-mdb-theme="auto">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="A modern availability calendar with stunning animations and design."
    />
    <meta name="author" content="Your Name" />
    <title>Availability Calendar</title>

    <!-- MDBootstrap CSS -->
    <link
      href="https://fonts.googleapis.com/css?family=Poppins:300,400,500,700&display=swap"
      rel="stylesheet"
    />
    <!-- MDB -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/8.1.0/mdb.min.css"
      rel="stylesheet"
    />
    <script
      type="text/javascript"
      src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.2.0/mdb.min.js"
    ></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/8.1.0/mdb.umd.min.js"></script>
    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />

    <!-- Custom styles -->
    <link href="<%= localhost %>/css/changecalender.css" rel="stylesheet" />

    <style>
      body {
        font-family: 'Roboto', sans-serif;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        margin: 0;
      }

      .content {
        flex: 1;
      }

      h1 {
        font-size: 3rem;
        color: #ffffff;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
        margin-bottom: 20px;
      }

      .calendar-header {
        background: rgba(255, 255, 255, 0.8);
        border-radius: 15px;
        padding: 20px;
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
      }

      #month-year {
        font-size: 1.5rem;
        font-weight: bold;
        color: #333;
      }

      .calendar {
        background: #ffffff;
        border-radius: 20px;
        padding: 20px;
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
      }

      .calendar-day {
        display: inline-block;
        width: calc(100% / 7 - 10px);
        height: 100px;
        margin: 5px;
        background: linear-gradient(135deg, #84fab0, #8fd3f4);
        color: #fff;
        font-size: 1.2rem;
        font-weight: bold;
        text-align: center;
        line-height: 100px;
        border-radius: 10px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        cursor: pointer;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .calendar-day:hover {
        transform: scale(1.1);
        box-shadow: 0 15px 25px rgba(0, 0, 0, 0.3);
      }

      .btn-custom {
        border: none;
        padding: 10px 20px;
        border-radius: 50px;
        transition: background 0.3s ease;
      }

      .btn-custom:hover {
        background: #ff6262;
      }

      .navbar {
        z-index: 1000;
      }

      .sidebar {
        width: 250px;
        background: linear-gradient(135deg, #ffffff 0%, #d8d8d8 100%);
        height: 100vh;
        color: rgb(0, 0, 0);
        position: fixed;
        top: 0;
        left: 0;
        padding: 20px 15px;
        box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
      }

      .sidebar .nav-link {
        color: rgb(0, 0, 0);
        font-weight: bold;
        display: flex;
        align-items: center;
        margin-bottom: 15px;
        transition: all 0.3s ease;
      }

      .sidebar .nav-link:hover {
        color: #ff6262;
        text-decoration: none;
        transform: translateX(5px);
      }

      .sidebar .nav-link i {
        margin-right: 10px;
      }

      .sidebar .brand {
        font-size: 1.5rem;
        font-weight: bold;
        margin-bottom: 30px;
        display: block;
        text-align: center;
        color: rgb(0, 0, 0);
      }

      .main-content {
        margin-left: 270px;
        padding: 30px;
        width: calc(100% - 270px);
        background-color: #f4f5f7;
      }

      .add-material,
      .materials-list {
        background: rgb(215, 215, 215);
        border-radius: 15px;
        padding: 20px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      .add-material h3,
      .materials-list h3 {
        font-weight: bold;
        color: #e8e8e8;
      }

      button {
        transition: all 0.3s ease;
      }

      button:hover {
        transform: scale(1.05);
      }

      table {
        margin-top: 15px;
      }

      table th {
        background-color: #f7f7f7;
        color: white;
      }

      table tr:nth-child(even) {
        background-color: #f9f9f9;
      }

      table tr:nth-child(odd) {
        background-color: #ffffff;
      }

      footer {
        background: linear-gradient(90deg, #36c114, #28a745);
        color: white;
        padding: 20px 0;
        text-align: center;
        font-size: 0.9rem;
      }
    </style>
  </head>
  <body>
    <%- include('../NavBar/NavBar') %> <%- include('SideNavBar') %>
    <!-- Theme Toggle -->

    <div class="content">
      <!-- Availability Calendar -->
      <div class="container mt-5 pt-5">
        <h1 class="text-center text-danger">Availability Calendar</h1>
        <div
          class="calendar-header d-flex justify-content-between align-items-center"
        >
          <button id="prev-month" class="btn btn-custom">
            <i class="fas fa-chevron-left"></i> Prev
          </button>
          <h2 id="month-year" class="text-center"></h2>
          <button id="next-month" class="btn btn-custom">
            Next <i class="fas fa-chevron-right"></i>
          </button>
        </div>
        <div class="calendar mt-4" id="calendar">
          <!-- Calendar days will be dynamically rendered here -->
        </div>
      </div>
    </div>
    <%- include('../Footer/footer') %>

    <!-- MDBootstrap JS -->
    <script
      type="text/javascript"
      src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.0/mdb.min.js"
    ></script>

    <!-- Custom Scripts -->
    <script src="<%= localhost %>/js/changecalender.js" defer></script>
  </body>
</html>
